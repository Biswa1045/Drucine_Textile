<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.biswa1045.drucine_textile">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Drucine_Textile">
        <activity android:name=".DirectActivity"></activity>
        <activity android:name=".CartActivity" />
        <activity android:name=".CatActivity" />
        <activity android:name=".SplashActivity" />
        <activity android:name=".PayActivity" />
        <activity android:name=".SummaryActivity" />
        <activity android:name=".CustomActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".OrdersActivity" />
        <activity
            android:name=".HomeActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Drucine_Textile.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>